# Market Pulse UI (React + Vite)

Frontend for the Market Pulse project.

## Requirements
- Node.js 18+
- Backend running locally (FastAPI)

## Setup
From this folder:

```bash
cd front/market-pulse-ui
npm install
npm run dev
